module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
    // fixes the 100vh postcss problem (content was only showing on 1/3rd of screen max)
    // require: "postcss-100vh-fix"
  },
}
